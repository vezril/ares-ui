apiVersion: v1
kind: Service
metadata:
  name: {{ include "ares-ui.fullname" . }}
  labels:
    {{- include "ares-ui.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "ares-ui.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
